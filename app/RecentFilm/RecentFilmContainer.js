import { connect } from 'react-redux'
import * as actions from '../actions'

import RecentFilm from './RecentFilm'

const mapStateToProps = state => state




export default connect(mapStateToProps, null)(RecentFilm)
